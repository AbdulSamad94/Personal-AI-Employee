{
  "mcpServers": {
    "email": {
      "command": "uv",
      "args": ["run", "mcp_servers/email/server.py"],
      "env": {
        "GMAIL_USER": "${GMAIL_USER}",
        "GMAIL_APP_PASSWORD": "${GMAIL_APP_PASSWORD}",
        "DRY_RUN": "${DRY_RUN}"
      }
    }
  }
}
